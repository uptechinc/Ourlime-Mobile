interface:
  display_name: "React Native Navigation & Safe Area Fix"
  short_description: "Safe area header rendering, Android status bar insets, keyboard handling, and stack slide transitions"
  default_prompt: "Use $react-native-navigation-fix for the user’s explicit navigation request. Stay within that scope and report adjacent route or UX work instead of implementing it without authorization."
