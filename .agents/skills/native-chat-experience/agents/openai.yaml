interface:
  display_name: "Native Chat Experience"
  short_description: "Build stable, themed native Ourlime chats"
  default_prompt: "Use $native-chat-experience for the user’s explicit chat request. Stay within that scope and report adjacent chat improvements instead of implementing them without authorization."
