interface:
  display_name: "Native Build Stability"
  short_description: "Standalone Android APK and iOS build diagnostic and stability procedures"
  default_prompt: "Use $native-build-stability to diagnose explicitly requested standalone build crashes, Firebase configuration, or TurboModules reflection issues. Do not run extra builds or change unrelated native code; report adjacent work separately."
